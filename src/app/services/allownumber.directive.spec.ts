import { AllownumberDirective } from './allownumber.directive';

describe('AllownumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AllownumberDirective();
    expect(directive).toBeTruthy();
  });
});
